# Training dependencies (Colab / GPU machine only)
# Install these in Google Colab, NOT in the HF Space Docker image.
# The HF Space only needs requirements.txt (server deps).

unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
trl>=0.12.0
datasets>=2.18.0
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.28.0
pandas
matplotlib
